# Pre-Push Audit — 1000-user readiness (2026-06-15)

_28 adversarially-verified findings across 9 domains (BE scale/money/security + FE onboarding/activation). 53-agent swarm._

**GO/NO-GO:** NO-GO until the must-fix list lands — two of the blockers (free-forever signups + the first-review soft-lock) directly defeat the monetization push and stall activation for the exact cohort you're driving in.

## Summary
The findings are real — I spot-verified the three highest-stakes ones in-repo: TokenBalance.unlimited defaults to True at models.py:60 with signals.py:29/31 creating rows without a defaults override, and the rehearsal token endpoint (views.py:177-198) mints a Daily.co join token with zero membership check. The biggest risk is that this push undermines its own goals two ways at once: every user you drive in after flipping AI_TOKEN_ENFORCEMENT signs up permanently free (silent 100% new-revenue leak you won't see because existing users cap correctly), and the headline free-review onboarding CTA soft-locks AI-cautious and non-uploading users on a dead screen they keep getting bounced back to. Stacked on top is a four-finding rehearsals IDOR cluster (token/join/detail/list) that lets any account drop into private live video reads and exfiltrate recordings — an Apple 1.2/5.1.1 and trust-destroying exposure — plus a RevenueCat webhook that fails open on unknown product_ids and processes sandbox purchases as real, both landmines given Android/Play and weekly tiers are in-flight with active TestFlight testing. Do first: flip the unlimited default + migration, then the first-review escape and onboarding skip/close (activation), then the rehearsals membership gates and the two RevenueCat fail-closed guards, then webhook idempotency. This is GO-WITH-FIXES on a ~1-2 day must-fix sprint; do not start driving users until at minimum the unlimited default, the first-review/onboarding escapes, and the rehearsals IDOR + RevenueCat sandbox/unmapped guards are in.

## Must-fix before push
### 1. [CRITICAL] New signups after the enforcement flip are permanently free (unlimited=True is the model default)
- **Where:** apps/subscriptions/models.py:60 (default); apps/users/signals.py:29,31; apps/subscriptions/utils.py:62,136; enable_token_limits.py:47-59
- **Fix:** Flip the model default to unlimited=False with a migration AlterField(default=False). Keep unlimited=True only on explicitly-comped/demo accounts (grant via grant_signup_tokens --set-unlimited and create_demo_user). enable_token_limits stays for the existing-row sweep, but the default MUST change so the post-flip cohort is capped. Confirmed live in code: model line 60 defaults True, signals.py:29/31 get_or_create has no defaults override.
### 2. [HIGH] First-review trap: new user who declines AI consent or doesn't upload is force-pinned to the Tape Review tab on every remount (no escape)
- **Where:** src/panels/Mobile/MobileApp.jsx:3097-3104, 3174, 3179; src/components/AIConsent/useAIGate.js:26-28; AuroraOnboarding.jsx:987
- **Fix:** Make the first-review screen escapable without paying or uploading: (1) wire the already-passed onExitFirstReview/back affordance into TapeReview, (2) in useAIGate on the mobile shell dismiss via drst-navigate/callback instead of navigate('/dashboard'); on decline in first-review mode clear dst_first_review + setFirstReviewActive(false) + setTab('home'), (3) clear dst_first_review in handleSetTab so tapping any bottom tab permanently exits first-review mode. This is the headline onboarding CTA and currently soft-locks AI-cautious users.
### 3. [HIGH] RevenueCat webhook fails OPEN on unknown product_id — grants active entitlement with no plan match
- **Where:** apps/subscriptions/views.py:411-425
- **Fix:** If plan is None, do NOT set status='active'. Log the unmapped product_id and return {'received':True,'ignored':'unmapped_product'} without mutating status. Critical because Android/Play products and the weekly tier are both in-flight — any product-id drift silently grants paid access.
### 4. [HIGH] RevenueCat webhook does not filter SANDBOX events — TestFlight/sandbox purchases grant real Premium
- **Where:** apps/subscriptions/views.py:391-429
- **Fix:** Read env = event.get('environment'); if env != 'PRODUCTION' return {'received':True,'ignored':'sandbox'} without mutating the subscription (gate behind a settings flag if intentional sandbox-against-prod testing is ever needed). During a 1000-user push with heavy TestFlight activity this is a trivial self-grant of Premium + unlimited tokens.
### 5. [HIGH] IDOR: RehearsalRoomTokenView mints a Daily.co join token for ANY room to ANY authenticated user
- **Where:** apps/rehearsals/views.py:172-198
- **Fix:** After loading room, add `if not _is_room_member(room, request.user): return 404` (helper already defined at line 354). Confirmed: endpoint only checks IsAuthenticated and is_owner is just a recording-rights flag, not a gate. Live-video privacy breach + Apple 1.2/5.1.1 exposure.
### 6. [HIGH] IDOR: RehearsalRoomJoinView lets any user join any room by id — no membership/block check, capacity race
- **Where:** apps/rehearsals/views.py:110-142
- **Fix:** Require an invite/host allow-list before creating the participant; call _is_blocked_between(room.host, request.user) and refuse on block; wrap capacity check + create in a transaction with select_for_update on the room. Without this, join-then-list-recordings enables recording exfiltration and bypasses Green Room block enforcement even after the token gate is fixed.
### 7. [HIGH] IDOR: RehearsalRoomDetailView/ListView leak every room's full record incl. room_url to any authenticated user
- **Where:** apps/rehearsals/views.py:96-107 and 60-65 (queryset RehearsalRoom.objects.all())
- **Fix:** Scope get_queryset to RehearsalRoom.objects.filter(Q(host=user)|Q(participants__user=user)).distinct(), or remove room_url from the serializer and only return it via the membership-gated token endpoint. Add pagination to the list view. Feeds the token/join IDORs above.
### 8. [HIGH] No webhook idempotency: Stripe/RevenueCat retries double-grant tokens to paying users
- **Where:** apps/subscriptions/views.py:244-354 (stripe), 370-452 (revenuecat); grant path utils.py:57-83
- **Fix:** Add a ProcessedWebhookEvent table with a unique constraint on the provider event id; get_or_create at the top of each handler inside a transaction and short-circuit if already seen. Make grant_monthly_tokens idempotent per (subscription, period). The existing clawback_inflated_tokens.py command proves over-granting already happened in prod once; at 1000-user Stripe retry volume this recurs and corrupts the usage_report numbers you're setting the soft cap from.
### 9. [HIGH] Onboarding soft-trap: required steps (identity/interests/goals/level) have no Skip and no global close
- **Where:** src/panels/Onboarding/AuroraOnboarding.jsx:1017 (onSkip only for notif/follow), 277/499/527/547, 909-915 (clamped back)
- **Fix:** Add a low-emphasis 'Set this up later' to identity/interests/goals/level (ProfileStep already has one) and/or a global X on the root overlay that calls finish()/onClose. Today a user who can't clear a validation (iOS keyboard/focus bug entering city, or refuses a role) is locked behind a full-screen modal with no exit — a direct activation blocker.

## Activation blockers
- First-review soft-lock (MobileApp.jsx:3097-3104 + useAIGate.js:26-28): the headline onboarding 'Try my free review' CTA pins AI-cautious users to a dead Tape Review tab on every remount with no non-paying, non-uploading escape — the exact cohort least likely to push through. MUST FIX.
- Inescapable onboarding modal (AuroraOnboarding.jsx:1017): identity/interests/goals/level have no Skip and there's no global X; a user who can't clear a required validation (iOS keyboard/focus bug, indecision) is locked out of the entire app behind the setup screen. MUST FIX.
- Mode toggle escapes the free-review flow into token-walled Compare Takes (TapeReview.jsx:108-145): a Day-0 free user with zero tokens can tap 'Compare takes' and hit an insufficient_tokens wall mid-activation, bypassing the FirstReviewPaywall. Force mode='single' while firstReview is true.
- Failed first review burns the one-time grant (utils.py:161-163 + no refund): if the server fails the analysis (provider outage, corrupt upload) the user's promised free aha is consumed with nothing delivered — kills the activation moment for an unknown slice of new users.
- Step-0 dead back chevron + duration-fallback degraded reviews are softer activation-trust erosions on first contact.

## Should-fix (non-blocking)
- _spend_token wrapper is fail-open (ai/views.py:53-78): narrow the except to ImportError/ModuleNotFoundError and fail CLOSED (503/402 + Sentry) on any other exception so DB contention at 1000 users doesn't gift free Claude calls.
- Tokens charged up-front and never refunded on analysis failure (ai/views.py:961,1166-1170,1056): charge after success or credit back on exception, and reset first_review_used on a failed first review so the promised free aha isn't burned. Compare Takes can burn 4 tokens for zero output.
- Stripe checkout activation swallows all exceptions (subscriptions/views.py:293-316): add an idempotent activation + a reconciliation command that repairs charged-but-inactive subscriptions; first cycle is otherwise lost silently.
- Community leaderboard materializes ALL actors per dashboard load (growth/views.py:346-427): slice rows[:50] server-side; this is the most likely first brownout on the single-box Railway worker pool.
- Synchronous tape/compare analysis pins a worker thread 60-120s with only ~3 ASGI workers (ai/selftape_analysis.py:116-174): move off the request path or cap CompareTakes concurrency before the analyzer scales to 1000 users.
- Community comment list is unpaginated + AllowAny (community/views.py:161-181): apply the same bounded window PostListCreateView already uses.
- Community feed doesn't filter blocked users (community/views.py:32-54,169-170): mirror the matching-app UserBlock exclusion — Apple 1.2 consistency gap.
- RoomInviteView lets any user push room_invite notifications (with room_url) to any user, ignoring blocks (rehearsals/views.py:201-249): require host/participant + block-check + rate limit.
- first_review free grant lacks select_for_update (utils.py:161-168): lock the TokenBalance row so a double-tap can't farm multiple free analyzer runs.
- extract_frames assumes a 30s tape when duration probe fails (selftape_analysis.py:121-127): use ffprobe JSON / fps-based pass and surface 'partial read' in _meta.
- Per-user list endpoints unpaginated (auditions/views.py) + ImportSubmissions has no row cap: add bounded newest-first windows.
- ReportContentView existence oracle over private message ids (community/views.py:207-298): verify reporter relationship for messages and return uniform success.
- Soft-cap COUNT-then-insert race (utils.py:99-112): defer given ~8 real actions/day, but tighten with an atomic per-day counter if abuse appears.
- CompareTakes error message says 300MB but rejects at 200MB (ai/views.py:1160-1163, 971-975): derive the message from MAX_BYTES_EACH.
- Onboarding step-0 back chevron is a dead button (AuroraOnboarding.jsx:114-121): hide it at qIndex===0 or wire it to onClose.
- useAIGate decline uses navigate(fallback) that can't dismiss mobile panels (useAIGate.js:17-29): pass a mobile-aware onDecline across all AI panels, not just tape-review.
- Wix webhook signature scheme doesn't match Wix's RS256 JWT (wix_sync/views.py:61-94): implement JWKS verification or formally document the webhook as disabled and rely on the scheduled pull.

## All confirmed findings (full)
### [HIGH] unlimited=True is the MODEL default — every new signup after the enforcement flip is permanently free  (be-billing)
- **Loc:** apps/subscriptions/models.py:60 (default); apps/users/signals.py:29,31 + apps/subscriptions/utils.py:62,136 (creation with no defaults); apps/subscriptions/management/commands/enable_token_limits.py:47-59
- **Issue:** TokenBalance.unlimited defaults to True. Every TokenBalance row is created via get_or_create(user=...) with NO defaults override (signals.py:29/31 on signup, utils.py:62/136, views.py:61/74/206), so every row inherits unlimited=True. In spend_token() (utils.py:148) `if balance.unlimited or _user_is_premium(user)` bypasses ALL deduction and only the soft cap can stop them. The flip helper enable_token_limits only does a ONE-TIME batch: `TokenBalance.objects.filter(unlimited=True).exclude(premium).update(unlimited=False)` over EXISTING rows. It never changes the model default. So the moment you set AI_TOKEN_ENFORCEMENT=true, every existing non-premium user is correctly limited — but every user who signs up AFTER the flip gets unlimited=True again and pays nothing, forever.
- **Impact:** At 1000 users with the paywall ON, the entire post-flip cohort (the people you're driving in the 2-week push) gets unlimited AI for free. The paywall silently leaks 100% of new-user revenue and you won't see it because existing users are correctly capped. This directly defeats the monetization launch.
- **Fix:** Change the model default to unlimited=False and add a migration AlterField(default=False). Reserve unlimited=True only for explicitly-comped/demo accounts set via grant_signup_tokens --set-unlimited. Verify signals.py and create_demo_user still set unlimited=True only where intended. Keep enable_token_limits for the existing-row sweep, but the default must flip so new signups are limited.
- **Verifier:** high — Confirmed directly from code. TokenBalance.unlimited has model default=True (models.py:60), and migration 0005_align_unlimited_default explicitly ALTERED the DB default from False (original in 0002) to True — so the default is True at both ORM and DB level. Every runtime row-creation site uses bare get_or_create(user=...) with NO defaults override: signals.py:29 and :31 (the new-signup post_save p
### [HIGH] No webhook idempotency: Stripe/RevenueCat retries double-grant tokens to Plus/Premium  (be-billing)
- **Loc:** apps/subscriptions/views.py:244-354 (stripe_webhook), 370-452 (revenuecat_webhook); grant path apps/subscriptions/utils.py:57-83
- **Issue:** Neither webhook records processed event IDs. Stripe explicitly retries events until it gets a 2xx and can deliver the same event more than once. checkout.session.completed (views.py:308) and invoice.payment_succeeded subscription_cycle (views.py:326) both call grant_monthly_tokens. For Plus/Premium (utils.py:64 ROLLOVER_PLANS), grant_monthly_tokens ADDS tokens (capped at 2x), so a retried/duplicated event grants again up to the rollover ceiling and writes a second 'subscription_renewal' ledger row. RevenueCat RENEWAL (views.py:428-429) has the same exposure. There is a signup_bonus idempotency guard (signals.py:27) but nothing equivalent on the paid grant path.
- **Impact:** Duplicate webhook deliveries (normal at Stripe scale, and guaranteed during any 500/timeout window) over-grant AI tokens to paying users, inflating your most expensive cost line and corrupting the token ledger / usage_report numbers you're using to set the soft cap.
- **Fix:** Add a ProcessedWebhookEvent table keyed on the provider event id (Stripe event['id'], RevenueCat event id), insert-on-process inside a transaction, and early-return if already seen. Make grant_monthly_tokens idempotent per (subscription, period) or gate it on the dedup table.
- **Verifier:** high — Confirmed from code. Neither webhook records a provider event id and there is no dedup table or unique constraint. apps/subscriptions/views.py stripe_webhook constructs event (line 237) but never persists event['id']; checkout.session.completed (line 308) and invoice.payment_succeeded with billing_reason=='subscription_cycle' (line 326) both call grant_monthly_tokens. revenuecat_webhook RENEWAL/IN
### [HIGH] RehearsalRoomTokenView hands a Daily.co join token for ANY room to ANY authenticated user (IDOR — strangers can join private live rehearsals)  (be-auth-idor)
- **Loc:** apps/rehearsals/views.py:172-198
- **Issue:** POST /rehearsals/<pk>/token/ loads the room by pk with NO membership check. Unlike RecordingsListView/RecordingAccessLinkView (which call _is_room_member at lines 380 and 423), the token endpoint only verifies the caller is authenticated, then mints a real Daily.co meeting token (create_daily_token) for room-name `rehearsal-<id>` and returns it along with room.room_url. A hostile new user can enumerate sequential room pks and pull a valid join token for every active rehearsal/audition video room. The host-vs-not check at line 184 only sets is_owner (recording rights) — it does NOT block non-members. Rooms allow max_participants=4.
- **Impact:** At 1000 users this is a live-video privacy breach: any account can drop into other actors' private scene reads / unreleased audition recordings by guessing room ids. Apple 1.2/5.1.1 exposure and a trust-destroying incident. The membership gate exists 30 lines away in the same file but was never applied here.
- **Fix:** After loading `room`, add `if not _is_room_member(room, request.user): return 404` (the helper is already defined at line 354). is_owner can remain as the recording-rights flag.
- **Verifier:** high — Confirmed at apps/rehearsals/views.py:172-198: RehearsalRoomTokenView loads the room by pk and mints a real Daily.co meeting token (create_daily_token, line 188) for room-name `rehearsal-<id>` for ANY authenticated user, returning it with room.room_url. The host check at line 184 only sets is_owner (recording rights), not access. The _is_room_member helper (line 354) IS applied to the recording en
### [HIGH] RehearsalRoomDetailView and ListView leak every room's full record (incl. room_url) to any authenticated user  (be-auth-idor)
- **Loc:** apps/rehearsals/views.py:96-107 and 60-65 (queryset RehearsalRoom.objects.all())
- **Issue:** GET /rehearsals/<pk>/ is a RetrieveAPIView with `queryset = RehearsalRoom.objects.all()` and only IsAuthenticated — no owner/member filter — and serializes the room in full. If RehearsalRoomSerializer exposes room_url (it is set on the model and returned by the public/token endpoints), any authenticated user can fetch the live Daily.co room URL for any room by id, feeding the token IDOR or enabling direct entry. RehearsalRoomListCreateView.get_queryset (line 60-65) likewise returns RehearsalRoom.objects.all() unscoped and unpaginated.
- **Impact:** Room-URL/PII leak of every room to the whole user base; combines with finding #1. The unscoped, unpaginated list also returns the entire room table — a brownout vector as room count grows toward 1000+ users.
- **Fix:** Scope get_queryset to RehearsalRoom.objects.filter(Q(host=user) | Q(participants__user=user)).distinct(), or remove room_url from the serializer and only return it via the membership-gated token endpoint. Add a bounded/paginated slice to the list view as community PostListCreateView already does.
- **Verifier:** high — Confirmed against the code. RehearsalRoomDetailView (views.py:96-107) is a RetrieveAPIView with `queryset = RehearsalRoom.objects.all()` and only IsAuthenticated, no owner/member filter. The serializer (serializers.py:32) includes `room_url` in `fields`, so GET /rehearsals/<pk>/ returns any room's Daily.co room_url to any logged-in user via sequential pk enumeration. RehearsalRoomListCreateView.ge
### [HIGH] RehearsalRoomJoinView lets any authenticated user join any room by id — no invite/membership check, no block-check, capacity TOCTOU race  (be-auth-idor)
- **Loc:** apps/rehearsals/views.py:110-142
- **Issue:** POST /rehearsals/<pk>/join/ get_or_create's a RehearsalParticipant for request.user on any room pk with no invitation/host-approval check and no _is_blocked_between gate. A user the host blocked (or who blocked the host) can still force into the room, bypassing the Green Room block enforcement matching/views.py applies via _is_blocked_between (line 690). Worse, the new participant row then satisfies _is_room_member, so a stranger joins (line 128) and can THEN legitimately pull recordings via RecordingsListView/RecordingAccessLinkView. Capacity check at line 122 is a separate read from the create at line 128, so concurrent joins race past max_participants.
- **Impact:** Block-bypass into private rooms plus self-escalation to recording-download access; at 1000 users the join-then-list-recordings chain enables full recording exfiltration even after the token gate (#1) is fixed. Capacity race lets rooms exceed their Daily cap.
- **Fix:** Require the joiner to be invited (verify a RoomInvite/notification exists) or on a host allow-list before creating the participant; call _is_blocked_between(room.host, request.user) and refuse on block; wrap the capacity check + create in a transaction with select_for_update on the room to close the race.
- **Verifier:** high — Confirmed in code. RehearsalRoomJoinView.post (apps/rehearsals/views.py:113-131) is IsAuthenticated-only and get_or_creates a RehearsalParticipant for request.user on ANY room pk with no invitation/host-approval check and no block gate. Rooms are enumerable: RehearsalRoomListCreateView.get_queryset returns RehearsalRoom.objects.all() (line 61) and PublicRoomDetailView is AllowAny. (1) No _is_block
### [HIGH] RevenueCat webhook fails OPEN on unknown product_id — grants active entitlement with no plan match  (be-integrations-webhooks)
- **Loc:** apps/subscriptions/views.py:411-425
- **Issue:** plan, billing = REVENUECAT_PRODUCT_MAP.get(product_id, (None, None)). When product_id is not in the map (a typo, a newly added App Store product, an Android product once Play billing lands, or a malformed event), plan is None. The `if plan:` guard at line 416 only skips updating sub.plan/sub.billing — but the INITIAL_PURCHASE/RENEWAL/UNCANCELLATION/PRODUCT_CHANGE branch at line 420 still unconditionally sets sub.status='active', cancel_at_period_end=False, and saves (line 421-425), regardless of whether the product was recognized. So an unrecognized product flips the user to 'active' on whatever plan already sat on their row (model default is 'basic', or a stale prior plan), granting entitlement that SubscriptionStatusView and the token logic read as a live paid subscription.
- **Impact:** Any product id drift between App Store Connect and REVENUECAT_PRODUCT_MAP (very likely the moment Android/Play products or weekly tiers go live — both are in-flight) silently grants paid access. Hard to detect because the user appears 'active' with a plausible plan. Inverse of fail-closed: an unmappable event should not confer entitlement.
- **Fix:** If plan is None, do not set status='active'. Log a warning with the unmapped product_id and return {'received': True, 'ignored': 'unmapped_product'} without mutating status. Only promote to active when plan resolves from the map.
- **Verifier:** high — Confirmed real at /Users/drselftape/Downloads/DrSelfTape_BE/apps/subscriptions/views.py:411-425. The line `plan, billing = REVENUECAT_PRODUCT_MAP.get(product_id, (None, None))` yields plan=None for any unmapped product_id. The `if plan:` guard at line 416 only skips updating sub.plan/sub.billing; the INITIAL_PURCHASE/RENEWAL/UNCANCELLATION/PRODUCT_CHANGE branch at line 420-425 unconditionally sets
### [HIGH] RevenueCat webhook does not filter SANDBOX events — test/sandbox purchases grant real entitlement  (be-integrations-webhooks)
- **Loc:** apps/subscriptions/views.py:391-429
- **Issue:** The handler reads event['type'], app_user_id, product_id, expiration_at_ms but never checks event['environment'] (RevenueCat sends 'SANDBOX' vs 'PRODUCTION' on every event). grep for environment/sandbox in views.py returns nothing. Any sandbox purchase — Apple sandbox tester, a TestFlight build using a StoreKit sandbox account, or RevenueCat's 'Send test event' button — arrives with a valid Authorization secret and a real Django app_user_id and is processed identically to a production purchase, granting status='active' and tokens.
- **Impact:** During a 1000-user push with active TestFlight testing, sandbox/StoreKit-test purchases will mint real Premium entitlements and unlimited-tier tokens for free, on production accounts. Also a trivial way for a savvy user on TestFlight to self-grant Premium.
- **Fix:** Read env = event.get('environment'). If env != 'PRODUCTION' (and you are not intentionally running a sandbox-allowing build), return {'received': True, 'ignored': 'sandbox'} without mutating the subscription. Gate behind a settings flag if sandbox testing against prod is ever needed.
- **Verifier:** high — Confirmed in code. apps/subscriptions/views.py:391-429 (revenuecat_webhook) reads event['type'], app_user_id, product_id, expiration_at_ms and never inspects event['environment']. grep -rni for environment/sandbox/PRODUCTION across apps/subscriptions/ returns nothing — no filter exists. On INITIAL_PURCHASE/RENEWAL the handler sets sub.status='active' and calls grant_monthly_tokens(user, plan) unco
### [HIGH] First-review trap: new user who declines AI consent (or doesn't upload) is force-pinned to the Tape Review tab on every remount  (fe-first-session)
- **Loc:** src/panels/Mobile/MobileApp.jsx:3097-3104, src/components/AIConsent/useAIGate.js:26-28
- **Issue:** Onboarding's "Try my free review" sets sessionStorage dst_first_review='1' (AuroraOnboarding.jsx:987) and drops the user into the tape-review tab in firstReview mode. On mount, MobileApp re-reads that flag (line 3099) and forcibly does setCurrentPanel(null)+setTab('tape-review') — on EVERY remount. The flag is only cleared in three places: upload start (TapeReview.jsx:157), upgrade (MobileApp.jsx:3174), and onExitFirstReview (3179). But onExitFirstReview is never callable (see separate finding), so the only non-paying escape is to actually upload a tape. Worse: TapeReview calls useAIGate() (TapeReview.jsx:85). A brand-new user has no ai_consent_accepted_at, so the consent modal opens; if they Decline, useAIGate runs navigate('/dashboard') (useAIGate.js:27). The mobile shell renders screens from internal `tab`/`currentPanel` state, NOT from the router (MobileApp.jsx:3347-3351), so that navigate does not dismiss the tape-review tab. The user is left staring at an upload form they can't use (consent denied → API 403s), with the flag still set, and any app foreground/remount yanks them straight back here.
- **Impact:** A meaningful slice of the 130-existing + new users who tap the onboarding free-review offer but don't want to consent-to-AI or upload a video get stuck on a dead screen they keep getting bounced back to. This is the single most prominent first-session CTA and it can soft-lock activation for AI-cautious users — exactly the cohort least likely to push through.
- **Fix:** Make the first-review screen escapable without paying or uploading: (1) wire a real onExitFirstReview/back affordance into TapeReview (it's passed but unused), (2) in useAIGate, when on the mobile shell, dismiss the panel/tab via the drst-navigate event or a callback instead of navigate('/dashboard'); on consent decline in first-review mode, clear dst_first_review and setFirstReviewActive(false)+setTab('home'), and (3) clear dst_first_review in handleSetTab so tapping any bottom tab permanently exits first-review mode.
- **Verifier:** high — Confirmed against code. MobileApp.jsx:3097-3104 has a mount-only effect that re-reads sessionStorage 'dst_first_review' and force-sets tab='tape-review' + firstReviewActive on every remount. The flag is set by AuroraOnboarding.jsx:987 ("Try my free review") and cleared only at upload-start (TapeReview.jsx:157), onUpgrade (MobileApp:3174), and onExitFirstReview (3179). Crucially, onExitFirstReview 
### [MEDIUM] _spend_token wrapper is fail-open: any exception lets the AI request through free  (be-billing)
- **Loc:** apps/ai/views.py:53-78 (esp. 77-78)
- **Issue:** The outer try/except in _spend_token catches ALL exceptions (`except Exception: return None`) and `return None` means 'OK, proceed'. This is intended to handle 'subscriptions app not available', but it also swallows DB errors, IntegrityError on the TokenTransaction insert, deadlocks under the atomic block, SoftCapReached subclass edge cases, OperationalError under load, etc. Every AI endpoint (cd_sim, live_scene, scene_generator, self_tape_review, take_compare — views.py:87,203,416,961,1168) calls this. Under DB contention at 1000 users, the failure mode is 'charge nobody, serve everybody'.
- **Impact:** Under load (connection-pool exhaustion, lock timeouts), spending fails open and AI calls run uncharged. Worse: a user can induce contention or hit transient DB errors to farm free Claude calls. AI is your most expensive per-call cost; this turns the cap into a soft suggestion exactly when scale stresses the DB.
- **Fix:** Narrow the outer except to ImportError/ModuleNotFoundError (the actual 'app not available' case). On any other exception, fail CLOSED — return a 503/402 JsonResponse and log to Sentry — so a broken ledger blocks the expensive call rather than gifting it.
- **Verifier:** high — Confirmed in code. apps/ai/views.py:77-78 is `except Exception: return None`, and in _spend_token `return None` signals "proceed" (callers do `if err: return err`). The inner try (lines 61-76) only catches SoftCapReached and ValueError. In apps/subscriptions/utils.py, spend_token runs inside `transaction.atomic()` doing get_or_create + TokenTransaction.create (lines 135-175). Any DB-layer exceptio
### [MEDIUM] Tokens are charged up-front and never refunded when analysis fails  (be-ai-analyzer)
- **Loc:** apps/ai/views.py:961 (SelfTapeReview), 1166-1170 (CompareTakes), 1056 (ParseSides); apps/subscriptions/utils.py:115-175 (spend_token has no refund path)
- **Issue:** _spend_token runs and commits the deduction BEFORE the expensive work. If analyze_self_tape / compare_takes / parse_sides_pdf raises (corrupt upload, Whisper down, Claude 5xx, worker timeout) the view returns 502/400 but the TokenTransaction(amount=-1) is already committed in its own transaction.atomic() (utils.py:135). There is no refund/credit-back anywhere. Compare Takes is worst: 4 separate spend_token calls (views.py:1167-1170) burn up to 4 tokens, then a single downstream failure 502s the whole request with zero output. spend_token also has no idempotency, so a client retry after a timeout double-charges.
- **Impact:** Paying users lose tokens for failures that are entirely the server's fault (AI provider outage, corrupt file the analyzer couldn't read, worker kill). At 1000 users with real AI-provider flakiness this is a steady stream of 'I paid and got nothing' complaints and chargebacks — and it directly poisons the free-first-review activation grant: a failed first review still burns the one-time first_review_used flag (utils.py:161-163) so the user never gets their promised free aha.
- **Fix:** Charge AFTER a successful analysis, or wrap in a try/refund: on any exception path, credit the token(s) back (a +1 TokenTransaction and balance bump) and, for the first-review grant, reset first_review_used. Add an idempotency key so a retried multipart upload doesn't double-charge.
- **Verifier:** high — Confirmed in code, not a misread. In SelfTapeReviewView (apps/ai/views.py:961), ParseSidesView (:1056), and CompareTakesView (:1167-1170) the token is charged via _spend_token BEFORE any expensive work. spend_token (apps/subscriptions/utils.py:135) commits the TokenTransaction(amount=-1) and balance.spend(1) inside its own transaction.atomic() that fully commits when it returns, before analyze_sel
### [MEDIUM] RoomInviteView lets any user push room_invite notifications (with room_url) for any room to any user — no host check, no block-check  (be-auth-idor)
- **Loc:** apps/rehearsals/views.py:201-249
- **Issue:** POST /rehearsals/invite/ takes arbitrary room_id + user_id and fires send_notification(room_invite) including room.room_url to the target. The caller does NOT need to be the room host (no `room.host == request.user` check) and there is no _is_blocked_between gate against the target. Any authenticated user can push notifications to any user_id (spam/harassment that ignores a block the target set) and leak any room's room_url to an arbitrary third party.
- **Impact:** Block-ignoring harassment channel (Apple 1.2) and a fan-out of room join URLs. At 1000 users a single script can blast unsolicited invites across the whole user table.
- **Fix:** Require request.user to be the room host or a participant (`if room.host_id != request.user.id and not room.participants.filter(user=request.user).exists(): return 403`). Add `if _is_blocked_between(request.user, target_user): return 403`. Rate-limit invites.
- **Verifier:** high — Confirmed in code. RoomInviteView.post (apps/rehearsals/views.py:204-249) accepts arbitrary room_id + user_id from any authenticated caller and calls send_notification(type="room_invite") with no host/participant check and no block check. Two proofs the gap is real, not guarded elsewhere: (1) the immediately following StartRecordingView (lines 265-269) enforces `if room.host != request.user: retur
### [MEDIUM] Community leaderboard loads ALL actors into memory and serializes the entire community on every dashboard load  (be-scale-concurrency)
- **Loc:** apps/growth/views.py:346-427 (CommunityLeaderboardView.get)
- **Issue:** `users = list(users_qs)` materializes every active actor with no LIMIT, then builds a per-user row dict, computes photo URLs (`_photo_url` calls request.build_absolute_uri per row), sorts the whole list in Python, and returns ALL rows in `data: rows` (no top-N slice). It already issues a per-call streak query, callbacks aggregate, ActorProfile lookup, and a snapshot scan over every user id. At 1000 users this is ~1000-row JSON + several full-table queries on a hot, repeatedly-polled dashboard panel.
- **Impact:** At 1000 users every Leaderboard fetch builds and ships a 1000-element payload and runs unbounded aggregates. Response size and CPU grow linearly with signups; combined with the dashboard firing ~10-15 calls/page (per the throttle comment) this is the endpoint most likely to brown out the single-box Railway worker pool first.
- **Fix:** Cap the board server-side: after sorting, slice `rows[:50]` (plus the caller's own row if outside the window) before returning. Keep `community_size`/`your_rank` computed from counts/aggregates, not from a fully-materialized list. Ideally precompute ranks in the existing LeaderboardRankSnapshot table and read a paginated slice instead of recomputing per request.
- **Verifier:** high — Confirmed at apps/growth/views.py:346-427 (CommunityLeaderboardView.get). For the default scope='all', `users = list(users_qs)` materializes EVERY active actor with no LIMIT (line 346), builds a row dict per user (lines 381-401), sorts the whole list in Python (line 406), and returns ALL rows in `leaders` with no top-N slice (line 418). So the unbounded-materialization + full-payload core of the f
### [MEDIUM] Synchronous self-tape analysis holds a worker thread for 60-120s doing CPU-bound ffmpeg + Whisper + LLM inline  (be-scale-concurrency)
- **Loc:** apps/ai/selftape_analysis.py:116-174 (extract_frames/extract_transcript) called synchronously from apps/ai/views.py tape-review/compare-takes POST handlers
- **Issue:** analyze_self_tape runs N ffmpeg frame extractions (each up to 30s timeout), an ffmpeg audio transcode (60s timeout), a Whisper API call, and a premium LLM call (up to 75s with adaptive thinking) — all synchronously inside the request. CompareTakes does this for up to 4 takes (MAX_TAKES=4, 200MB each) in one request. ffmpeg is CPU-bound and Railway runs a small vCPU count.
- **Impact:** Each tape analysis pins a worker thread for ~1-2 minutes; a Compare-Takes of 4 takes can run several minutes. With only ASGI_WORKERS=3 gunicorn processes, a handful of concurrent analyzer users (the exact feature being launched for 1000 users) can exhaust available worker capacity and stall unrelated requests (login, dashboard). gunicorn --timeout 120 will also kill long Compare-Takes runs mid-analysis.
- **Fix:** Move tape/compare analysis off the request path: enqueue to a background worker (Celery/RQ/Railway cron-worker) and have the client poll a job-status endpoint, OR at minimum cap CompareTakes concurrency and raise/segment the gunicorn timeout for that route. The ai_feedback throttle (20/hour) limits one user but not aggregate worker saturation.
- **Verifier:** medium — Confirmed the core structural claim: SelfTapeReviewView.post (views.py:957) and CompareTakesView.post (views.py:1147) are synchronous DRF APIView handlers that call analyze_self_tape / compare_takes inline. Under the deployed runtime (Procfile + railway.json: gunicorn self_tape_api.asgi:application -k uvicorn.workers.UvicornWorker --workers ${ASGI_WORKERS:-3}), Django's get_asgi_application runs s
### [MEDIUM] Community comment list endpoint is unpaginated and public (AllowAny)  (be-scale-concurrency)
- **Loc:** apps/community/views.py:161-181 (PostCommentListCreateView), get_queryset :169-170 / list :175-181
- **Issue:** PostCommentListCreateView.list serializes the entire comment set for a post with no limit, and GET is AllowAny (apps/community/views.py:164-167). The parent feed (PostListCreateView) was correctly capped to a 50/100 window, but comments were not given the same treatment.
- **Impact:** A popular/pinned post that accumulates hundreds of comments returns the whole thread on every open, unauthenticated and uncached. At 1000 users a single hot thread becomes a cheap way to pull a large response repeatedly with no auth gate.
- **Fix:** Apply the same bounded limit/offset window used in PostListCreateView.list (apps/community/views.py:74-82) to the comment list, defaulting newest-first with a max page size.
- **Verifier:** high — Confirmed in code. PostCommentListCreateView.list (apps/community/views.py:175-181) serializes the entire comment queryset for a post with no limit/offset slicing, and GET is explicitly AllowAny (views.py:164-167). The sibling PostListCreateView.list (views.py:74-82) IS bounded to a 1-100 newest-first limit/offset window, so the asymmetry the finding describes is real. There is no DEFAULT_PAGINATI
### [MEDIUM] Community feed does not filter out blocked users' posts/comments  (be-scale-concurrency)
- **Loc:** apps/community/views.py:32-54 (PostListCreateView.get_queryset) and :169-170 (PostCommentListCreateView.get_queryset)
- **Issue:** DiscoverView, MatchListView and the messaging endpoints all carefully exclude UserBlock pairs (apps/matching/views.py:76-80, :243-252, :690-718), but the community Post/Comment feeds do not. A user who blocked another still sees that person's posts and comments in the feed.
- **Impact:** Not a scale-out failure, but an Apple-1.2 UGC consistency gap that surfaces more as the community grows toward 1000 users and blocking becomes common; the rest of the app enforces blocks and the feed silently doesn't.
- **Fix:** In PostListCreateView/PostCommentListCreateView get_queryset, when the requester is authenticated, exclude authors in the union of UserBlock(blocker=me) and UserBlock(blocked=me), mirroring the matching-app pattern. Guard for the AllowAny anonymous case.
- **Verifier:** high — Confirmed by reading the cited code. PostListCreateView.get_queryset (apps/community/views.py:32-54) builds the feed with select_related, a like-prefetch, and optional type/topic filters but applies NO UserBlock exclusion. PostCommentListCreateView.get_queryset (:169-170) is just PostComment.objects.filter(post_id=...).select_related('author') with no block filter. The serializers (apps/community/
### [MEDIUM] Subscription webhooks have no event-level idempotency — replays double-grant tokens  (be-integrations-webhooks)
- **Loc:** apps/subscriptions/views.py:230-354 (stripe_webhook), :318-328 (invoice.payment_succeeded), :370-452 (revenuecat_webhook)
- **Issue:** Neither the Stripe nor RevenueCat handler records processed event IDs. There is no WebhookEvent/processed-event table anywhere in apps/ (grep for event_id/processed_event/idempotency returns only the unrelated growth booking flow). grant_monthly_tokens (utils.py:57) unconditionally adds tokens every time it runs. Stripe documents that it retries webhook deliveries (network blips, slow responses, manual replays in the dashboard), and any retried/duplicated 'invoice.payment_succeeded' with billing_reason='subscription_cycle' (views.py:325) or 'checkout.session.completed' (views.py:308) re-runs grant_monthly_tokens for the same billing period. RevenueCat likewise re-sends events. For rollover plans (plus/premium) this stacks tokens up to the cap on every duplicate; for the soft-cap accounting it pads total_earned. The existence of apps/subscriptions/management/commands/clawback_inflated_tokens.py — written specifically to undo 'inflated token balances introduced by the pre-patch grant bugs' — confirms over-granting has already happened in production once.
- **Impact:** At 1000 users with real Stripe/RevenueCat retry volume, duplicate delivery is routine, not rare. Users accumulate free AI tokens they didn't pay for, eroding the entire token-economy / soft-cap model the monetization plan depends on, and forcing repeated manual clawback runs. The whole point of turning enforcement on (Unlimited plan rollout) is defeated if grants aren't idempotent.
- **Fix:** Add a ProcessedWebhookEvent model keyed on the provider event id (Stripe event['id']; RevenueCat event.get('id')) with a unique constraint. At the top of each handler, get_or_create the row inside a transaction and short-circuit (return 200) if it already existed. Alternatively/additionally make grant_monthly_tokens idempotent-per-period by recording a TokenTransaction unique on (user, reason, period_key) and skipping if present.
- **Verifier:** high — CONFIRMED REAL but over-stated; corrected severity high→medium.

Core claim verified in code: there is no event-level idempotency anywhere in apps/subscriptions. grep for event_id/processed_event/ProcessedWebhook/idempotency returns ONLY the unrelated growth booking flow (apps/growth/models.py:142, views.py:544). Both webhook handlers re-run grant_monthly_tokens with no dedup:
- stripe_webhook che
### [MEDIUM] Stripe checkout activation swallows all exceptions — charged user can be left with no active subscription and no retry  (be-integrations-webhooks)
- **Loc:** apps/subscriptions/views.py:293-316
- **Issue:** checkout.session.completed wraps the entire activation (Subscription.retrieve + sub.save + grant_monthly_tokens) in a bare `except Exception` that only logs and returns 200 (line 309-316). The comment correctly notes Stripe would retry-storm a 500 — but because the handler also has no idempotency key AND no separate reconciliation path, a transient failure here (Stripe API timeout on the retrieve at line 297, a DB hiccup) means: Stripe sees 200, never re-delivers, and the user who was just charged is never activated. There is no fallback that reconciles 'charged-but-inactive' subscriptions (e.g. a periodic sweep over Stripe subscriptions). The only other token-granting path, invoice.payment_succeeded, only grants on billing_reason='subscription_cycle' (line 325), NOT the first invoice — so the first cycle is lost entirely if checkout.session.completed errors.
- **Impact:** At scale, a small but nonzero fraction of paying signups silently end up paid-but-locked-out — the worst activation/trust failure for a freshly-charged user, and it surfaces as support tickets, not alerts you can act on quickly.
- **Fix:** Keep returning 200 (don't retry-storm), but (a) make activation idempotent so a safe retry path exists, and (b) add a reconciliation job/management command that lists active Stripe subscriptions whose local UserSubscription is missing/inactive and repairs them. Also consider activating from invoice.payment_succeeded on billing_reason='subscription_create' as a backstop.
- **Verifier:** high — Confirmed at apps/subscriptions/views.py:293-316. The first-signup web activation (get_or_create UserSubscription → stripe.Subscription.retrieve → sub.save with status='active' → grant_monthly_tokens) is wrapped in a bare `except Exception` that only log.exception()s and lets the handler return JsonResponse({'received': True}) (200) at line 354. A transient failure (Stripe retrieve timeout at L297
### [MEDIUM] First three required steps (identity/profile/interests/goals/level) have no Skip — only notif/follow do — so a user who can't satisfy validation is soft-trapped with no way out of onboarding  (fe-onboarding-buttons)
- **Loc:** src/panels/Onboarding/AuroraOnboarding.jsx:1017 (onSkip only for notif/follow), 277/499/527/547 (Continue disabled until valid)
- **Issue:** onSkip is wired only for 'notif' and 'follow' (line 1017). identity, profile, interests, goals, and level show a disabled Continue until their validation passes (name+name, city, >=1 selection, etc.). ProfileStep alone offers a GhostBtn 'I'll finish this later' (line 502) that calls onNext regardless. The other four required steps have NO escape: no skip, no close, back is clamped. If a user genuinely can't/won't provide a required field (e.g. keyboard issues entering city, or refuses to pick a role), they cannot proceed and cannot exit the full-screen overlay. There is no X/dismiss on the root overlay at all.
- **Impact:** At scale, any user who hits a validation they can't clear (input focus/keyboard bugs on iOS, indecision, accidental entry into onboarding) is locked out of the entire app behind a modal they can't close. This is a direct activation blocker and likely shows up as 'app is stuck on the setup screen' support reports.
- **Fix:** Add a Skip affordance (or a low-emphasis 'Set this up later') to identity/interests/goals/level the way profile already has, and/or add a global close (X) on the root overlay that calls finish()/onClose so onboarding is never an inescapable modal. Driving everything through component state is correct — just ensure at least one always-enabled exit per step.
- **Verifier:** high — Confirmed in code. AuroraOnboarding renders as a fixed full-screen overlay (zIndex 1000) with no root-level close/X. onSkip (line 1017) is wired only for 'notif' and 'follow'. back() is clamped at step 0 (go() uses Math.max(0,...)), so the very first step (identity) has no back. The only ways out of the whole flow are finish() — reachable only by completing through to welcome/offer — or the parent
### [LOW] first_review free grant is not select_for_update'd — race lets a user farm multiple free analyzer runs  (be-billing)
- **Loc:** apps/subscriptions/utils.py:161-168 (the comment at 159-160 acknowledges select_for_update is missing)
- **Issue:** The free-first-review branch reads balance.first_review_used (loaded by get_or_create at line 136, NOT locked) and only then sets it. The code comment claims 'get_or_create above already serializes inside the atomic block' — that is false; get_or_create does not take a row lock on an existing row, it just SELECTs. Two concurrent self_tape_review requests (views.py:961) for the same user both read first_review_used=False, both pass the check, both run a free Claude analysis, then both write first_review_used=True. Easily triggered by a double-tap or two tabs.
- **Impact:** Limited-tier users with an empty balance can farm N free Tape Reviews (the most expensive AI action — vision parse + analysis) by firing concurrent requests. Multiplied across 1000 users, that's a real Claude bill and undermines the very paywall the free-first-review is meant to sell past.
- **Fix:** Replace the unlocked get_or_create read with TokenBalance.objects.select_for_update().get(pk=...) inside the atomic block before checking first_review_used, so concurrent requests serialize on the row. Re-check first_review_used after acquiring the lock.
- **Verifier:** high — The race condition is real in code. apps/subscriptions/utils.py:136 uses TokenBalance.objects.get_or_create(user=user), which on an existing row issues a plain unlocked SELECT. Under PostgreSQL READ COMMITTED (Railway default), transaction.atomic() gives atomicity but NOT mutual exclusion, so the inline comment at lines 159-160 ("get_or_create above already serializes inside the atomic block") is 
### [LOW] Soft-cap check is a non-atomic COUNT — concurrent requests overshoot the daily fair-use ceiling  (be-billing)
- **Loc:** apps/subscriptions/utils.py:99-112 (_soft_cap_exceeded) called at 149
- **Issue:** _soft_cap_exceeded does a COUNT of spend rows in the last 24h, then spend_token inserts a new row. Between the COUNT and the INSERT there is no lock, so K concurrent requests from one Premium user all read used=cap-1 and all proceed, inserting K rows past the cap. The cap is fair-use (150/day) so this is bounded and low-stakes, but it means the 'fair use' ceiling is soft to bursts.
- **Impact:** A scripted/burst Premium user can exceed the daily AI fair-use cap by their concurrency factor. Bounded impact (cap is generous vs. ~8 real actions/day per usage data) but it weakens the only backstop on unlimited-tier cost.
- **Fix:** Acceptable to defer given real usage is ~8/day, but if tightened: gate the count+insert behind a select_for_update on the TokenBalance row, or move daily usage to an atomic counter (F()-incremented per-day row) instead of a COUNT-then-insert.
- **Verifier:** high — Confirmed the race is real. In apps/subscriptions/utils.py, _soft_cap_exceeded (lines 99-112) does a non-locking .count() of TokenTransaction spend rows in the rolling 24h window, and spend_token calls it at line 149 then INSERTs the new spend row at lines 151-153. Although this runs inside transaction.atomic() (opened line 135), nothing in the unlimited/Premium branch takes a lock that would seri
### [LOW] Error-message size limits contradict the enforced caps (user-facing confusion at the cap)  (be-ai-analyzer)
- **Loc:** apps/ai/views.py:1160-1163 (CompareTakes) and 971-975 (SelfTapeReview)
- **Issue:** CompareTakes enforces MAX_BYTES_EACH = 200MB (views.py:1144) but the rejection message says 'Each take must be under 300 MB.' (views.py:1162). A user with a 250MB take is told the limit is 300MB yet gets rejected at 200MB.
- **Impact:** Users near the cap get a contradictory error and retry/abandon, generating support noise during the activation flow. Minor but user-facing at exactly the wrong moment.
- **Fix:** Make the message match the constant (derive it from MAX_BYTES_EACH) for both endpoints.
- **Verifier:** high — Confirmed at apps/ai/views.py: CompareTakes sets MAX_BYTES_EACH = 200 * 1024 * 1024 (200 MB) at line 1144, but the rejection message at line 1162 reads "Each take must be under 300 MB." A take between 200-300 MB is rejected while being told the limit is 300 MB — a genuine message/constant mismatch. However, the finding overstates scope: the SelfTapeReview endpoint (lines 955-975) is NOT contradict
### [LOW] extract_frames falls back to assuming a 30s tape when duration probe fails — silent quality degradation on a class of files  (be-ai-analyzer)
- **Loc:** apps/ai/selftape_analysis.py:121-127 (duration<=0 -> duration=30.0)
- **Issue:** If _probe_duration returns 0 (probe timeout or an ffmpeg banner format it can't parse — regex at selftape_analysis.py:107), extract_frames blindly assumes a 30s tape and seeks into it. For a 3-minute take this samples only the first ~28s; for a 10s take it seeks past the end and several frame grabs produce nothing. The analysis still 'succeeds' and a token is charged, but the coach is reasoning over a non-representative or near-empty frame set, and _meta.duration_s reports 0.
- **Impact:** A subset of valid tapes (unusual containers, variable-bitrate, certain phone exports) get a quietly degraded review the user paid a token for. Hard to detect because it returns 200, not an error. At 1000 users with heterogeneous phone exports this is a steady trickle of 'the notes don't match my tape' confusion.
- **Fix:** If duration probe fails, prefer ffprobe/JSON duration or extract frames via a single fps-based pass rather than guessing 30s; and surface 'partial read' in _meta so the client can warn or offer a free re-run.
- **Verifier:** high — All technical claims verified against the code. apps/ai/selftape_analysis.py:121-127: when _probe_duration() returns 0.0, extract_frames() blindly assumes duration=30.0. With SAMPLE_PAD=0.08 and NUM_FRAMES=8, frames are seeked from start=2.4s to end=27.6s. For a 3-min take this samples only the first ~15% (non-representative); for a 10s take several seeks land past EOF and those grabs are silently
### [LOW] ReportContentView is an existence oracle over arbitrary private GreenRoomMessage/Post/Comment ids and accepts reports against unrelated users  (be-auth-idor)
- **Loc:** apps/community/views.py:207-298
- **Issue:** _resolve_target_user lets a caller pass any target_object_id for target_type message/post/comment; the endpoint returns 404 'Could not locate the reported content' when the id doesn't resolve vs 201 when it does — an existence oracle over other users' private Green Room message ids. It never verifies the reporter has any relationship to the message (e.g. is a participant of that match), so a user can probe which message ids exist and file reports against users they've never interacted with.
- **Impact:** Low-severity info leak / report-spam: enumerate private message-id existence and pollute the moderation inbox with reports against arbitrary users. Message bodies are not returned, so ranked low.
- **Fix:** For target_type='message', load GreenRoomMessage.match and verify request.user is user1/user2 before accepting. Return a uniform success/202 regardless of whether the object resolved to remove the oracle, and de-dupe reports per (reporter, target).
- **Verifier:** high — Confirmed in apps/community/views.py:207-321. _resolve_target_user (lines 207-226) catches the DoesNotExist and returns None for any unresolvable id; post() (lines 277-283) then returns 404 'Could not locate the reported content', whereas a resolvable id falls through to a 201 ContentReport.create (lines 291-320). This is a genuine differential-response existence oracle. For target_type='message' 
### [LOW] Per-user list endpoints (auditions, submissions, scripts, materials, tracker) have no pagination cap  (be-scale-concurrency)
- **Loc:** apps/auditions/views.py:36-42 (AuditionSlotListCreateView.list), :229-235 (SubmissionListCreateView.list), :358-406 (AuditionTrackerView.get), :574-589 (SelfAuditionListView), :595-642 (SelfMaterial views)
- **Issue:** These list endpoints return the user's full queryset with no slice. ImportSubmissionsView (apps/auditions/views.py:775-847) bulk-creates one AuditionSlot/TapeSubmission per row of a parsed agency report with no row cap, and PromoteToAudition/agency imports can grow a single actor's slot table without bound. AuditionTrackerView additionally splits notes line-by-line in Python for every row.
- **Impact:** Bounded per-user today, but a power user importing large/repeated Eco Cast reports accumulates thousands of slots; their tracker/list/stats calls then return unbounded payloads and the dashboard (which fires these on every load) does ever-growing per-request work. Not a 1000-user-wide brownout, but a per-account degradation that gets reported as 'app is slow'.
- **Fix:** Add a bounded newest-first window (e.g. limit/offset, max 100) to the list endpoints the same way community PostListCreateView already does it (apps/community/views.py:68-87), and cap ImportSubmissions row count per request.
- **Verifier:** high — Confirmed against the code. All five cited list endpoints serialize the full per-user queryset with many=True and no slice: AuditionSlotListCreateView.list (views.py:36-42), SubmissionListCreateView.list (:229-235), SelfAuditionListView (:581-589), SelfMaterialListCreateView.list (:605-613), and SelfMaterialAuditionMaterialsView (:634-642). AuditionTrackerView.get (:361-406) loads the entire per-u
### [LOW] Wix webhook signature scheme does not match how Wix actually signs — genuine events 401  (be-integrations-webhooks)
- **Loc:** apps/wix_sync/views.py:61-94
- **Issue:** The handler computes an HMAC-SHA256 over the raw body with WIX_API_KEY and compares to x-wix-signature. The code's own comment (lines 66-69) admits this does NOT match Wix's real signing (Wix sends an RS256 JWT verified against its JWKS), so real Wix events will be rejected with 401 until JWT verification lands. The endpoint correctly fails CLOSED (503 when no secret, 401 on mismatch) and the downstream _upsert_booking is idempotent (services.py:275 update_or_create keyed on wix_booking_id), so there's no double-grant or entitlement risk — bookings are not entitlements.
- **Impact:** Real-time booking sync via webhook is effectively non-functional; bookings only reconcile via the manual/scheduled pull (ManualSyncView / sync_wix command). Not a money or entitlement bug, but a silently-broken integration to be aware of before relying on it at 1000 users.
- **Fix:** Implement the RS256 JWT verification against Wix's JWKS (or formally rely on the scheduled pull and document the webhook as disabled). Keep the fail-closed behavior. No idempotency change needed — the upsert is already keyed correctly.
- **Verifier:** high — Confirmed against apps/wix_sync/views.py:54-94 and apps/wix_sync/services.py:275. The webhook handler computes HMAC-SHA256 over the raw body with WIX_API_KEY and compares to the x-wix-signature header. The code's own comment (lines 66-69) admits Wix actually signs with an RS256 JWT verified against its JWKS, and a repo-wide grep confirms no JWT/JWKS verification exists in wix_sync (JWKS verificati
### [LOW] Back arrow on the first step (identity) is a visible dead button — go(-1) clamps to 0 and silently no-ops  (fe-onboarding-buttons)
- **Loc:** src/panels/Onboarding/AuroraOnboarding.jsx:114-121 (TopBar back button), 909-915 (go/back), 1016 (onBack=back)
- **Issue:** TopBar always renders the back chevron, including on step 0 (identity). back() calls go(i-1)=go(-1), which is clamped by Math.max(0,...) at line 910 to 0, so it does nothing. The arrow looks tappable but is inert on the very first screen. There is also no close/X anywhere in the Q-flow, so a user who landed in onboarding and taps the only top-left affordance gets zero feedback.
- **Impact:** Minor confusion at the first impression; a user expecting the back arrow to dismiss onboarding finds it dead. Not a hard trap but erodes trust on screen one.
- **Fix:** Hide the back chevron when qIndex===0 (render the spacer div like the no-skip branch does), or have back() on step 0 invoke onClose to dismiss onboarding.
- **Verifier:** high — All cited facts verified in src/panels/Onboarding/AuroraOnboarding.jsx. STEPS (line 24) and Q_STEPS (line 25) both begin with 'identity', so on the first onboarding screen qIndex === 0. TopBar (lines 107-144) renders the back chevron button unconditionally — onClick={onBack}, cursor:'pointer' (lines 114-117), with no qIndex guard. It is wired via onBack={back} (line 1016). back() = go(i - 1) = go(
### [LOW] Mode toggle lets a new user escape the free-review flow into token-walled Compare Takes with zero tokens  (fe-activation-path)
- **Loc:** src/panels/Dashboard/Jericho/TapeReview.jsx:108-135, 138-145 (CompareTakes branch)
- **Issue:** The single/compare mode toggle (modeToggle) renders in BOTH modes and is shown even when firstReview is true. A brand-new user dropped onto the free-review screen can tap 'Compare takes', which renders <CompareTakes /> (line 142). CompareTakes has no firstReview awareness and no free entitlement — it is purely token-gated ('Uses 1 token per take', CompareTakes.jsx:326-327). A Day-0 free user has no tokens, so this is an immediate dead-end / 'insufficient_tokens' wall in the middle of the curated free moment, and it bypasses the FirstReviewPaywall the flow is built around. firstReview mode also never funnels the user toward the single-take path it expects.
- **Impact:** An off-ramp out of the activation flow into a paywalled-but-not-paywall-screen feature during the first session — confusing, breaks the intended single-take narrative, and can hard-stop the user on a no-tokens error instead of delivering their promised free review.
- **Fix:** When firstReview is true, hide or disable the Compare-takes toggle (force mode='single') so the new user stays on the single curated free-review path. Re-enable Compare only after the first review completes / they're a normal user.
- **Verifier:** medium — Confirmed structurally in the code. In TapeReview.jsx the `modeToggle` (which contains the "Compare takes" button, line 119-124) is rendered unconditionally in both the submit-form state (line 335) and the compare branch (line 141), with NO check on the `firstReview` prop. The `mode === 'compare'` branch (line 138) renders `<CompareTakes />` (line 142), and CompareTakes.jsx takes no firstReview pr
### [LOW] AI-consent decline relies on navigate(fallback) which does not dismiss any mobile panel  (fe-first-session)
- **Loc:** src/components/AIConsent/useAIGate.js:17-29; consumers: AuditionGenerator/index.jsx:119, SceneStudy/index.jsx:89, CDSim/index.jsx:192, CastingDirectorAI/index.jsx:633, Jericho/index.jsx:161, TapeReview.jsx:85
- **Issue:** useAIGate's decline path is `if (!ok) navigate(fallback)` with fallback default '/dashboard'. Every AI panel on mobile (Generator, Practice/SceneStudy, Acting Coach/CDSim, Jericho, Tape Review) is rendered inside MobileApp via setCurrentPanel/setTab state (PanelScreen at MobileApp.jsx:2828/3347), not via the router. navigate('/dashboard') changes the route but the mobile shell is mounted at the app root and keeps rendering whatever `tab`/`currentPanel` say, so the declined panel stays on screen in a half-broken 'AI rejected by BE' state — the exact state the hook's doc comment says it exists to avoid. For panels reached via setCurrentPanel the PanelScreen back chevron still works, so it's recoverable; for the tape-review TAB (no back chevron, no X) it is not.
- **Impact:** Any AI-cautious user who declines consent lands on a non-functional AI screen. On the Generator/Coach panels they can back out; on the Tape Review tab they cannot. Erodes trust on first contact with the headline AI features.
- **Fix:** Pass a mobile-aware dismiss into useAIGate (e.g. an onDecline callback that fires drst-navigate to 'home' or calls the panel's onBack) instead of router navigate. At minimum special-case the tape-review tab to exit first-review + return to home on decline.
- **Verifier:** high — The core mechanism is confirmed. useAIGate's decline path (src/components/AIConsent/useAIGate.js:27) calls navigate(fallback='/dashboard'). On mobile, src/panels/Dashboard/DashboardLayout.jsx:10-11 returns <MobileApp/> for the entire /dashboard route subtree (no <Outlet/> on mobile), and routes/config.jsx mounts DashboardLayout at /dashboard with the AI panels (cd-sim, scene-study, jericho, genera